#ifndef GLOBAL_CONFIG_H
#define GLOBAL_CONFIG_H

const int STEP = 10;
const int RECONNECT_STEP = 1000;
const int FIXED_DELTA_TIME = 100;

const double Pi = 3.1415926535897;

#define ABS(x) ((x) > 0 ? (x) : (-(x)))
#define SQR(x) ((x) * (x))

#endif